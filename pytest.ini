[pytest]
markers =
    unit: fast unit tests (no network, no disk I/O beyond tmp_path)
    integration: integration tests that may perform real I/O or network access
    withoutresponses: tests that require real HTTP access (bypasses responses mock)
