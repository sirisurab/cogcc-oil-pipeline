[pytest]
minversion = 7.0
addopts = -v --tb=short
testpaths = tests
markers =
    unit: tests that require neither network access nor data files on disk
    integration: tests that require network access or data files on disk
